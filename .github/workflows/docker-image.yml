name: Docker Image CI

on:
  push:
    branches: [ "spring_boot" ]
  pull_request:
    branches: [ "spring_boot" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag voting-app:$(date +%s)
